import sendMove  from "../fetchers/move.js";
import startNewRound from "../fetchers/newRound.js";
import disconnectPlayer from "../fetchers/disconnect.js";

export const fetchers = {
  sendMove,
  startNewRound,
  disconnectPlayer,
};
